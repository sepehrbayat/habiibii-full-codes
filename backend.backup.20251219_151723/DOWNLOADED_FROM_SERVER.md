# ✅ پروژه‌ها مستقیماً از سرور دانلود شدند

**تاریخ دانلود:** ۱۷ دسامبر ۲۰۲۵  
**منبع:** سرور (193.162.129.214)  
**مقصد:** `/var/www/`

## 📥 فرآیند دانلود

### Laravel
- **منبع:** `root@193.162.129.214:/var/www/6ammart-laravel/`
- **مقصد:** `/var/www/6ammart-laravel/`
- **روش:** `rsync` مستقیم از سرور

### React
- **منبع:** `root@193.162.129.214:/var/www/6ammart-react/`
- **مقصد:** `/var/www/6ammart-react/`
- **روش:** `rsync` مستقیم از سرور

## ✅ تطبیق کامل با سرور

| پروژه | مسیر سرور | مسیر لوکال | وضعیت |
|-------|-----------|------------|-------|
| **Laravel** | `/var/www/6ammart-laravel` | `/var/www/6ammart-laravel` | ✅ یکسان |
| **React** | `/var/www/6ammart-react` | `/var/www/6ammart-react` | ✅ یکسان |

## 🔍 بررسی‌های انجام شده

### تعداد فایل‌ها
- ✅ تعداد فایل‌های Laravel: مطابق با سرور
- ✅ تعداد فایل‌های React: مطابق با سرور
- ✅ تعداد دایرکتوری‌ها: مطابق با سرور

### فایل‌های کلیدی
- ✅ Laravel: `artisan`, `config/cors.php`, `app/Http/Controllers/Api/V1/ConversationController.php`, `app/CentralLogics/helpers.php`
- ✅ React: `package.json`, `src/`, `src/utils/safeRender.js`
- ✅ Beauty Module: فایل‌های vendor hooks موجود هستند

### Checksum
- ✅ Checksum فایل‌های کلیدی با سرور یکسان است

## 📝 تفاوت با روش قبلی

**قبلاً:** کدها از `/home/sepehr/Projects/` کپی می‌شدند  
**حالا:** کدها **مستقیماً از سرور** دانلود شدند

این اطمینان می‌دهد که:
- ✅ کدها دقیقاً همان چیزی هستند که روی سرور است
- ✅ هیچ تفاوتی بین سرور و لوکال وجود ندارد
- ✅ مسیرها دقیقاً یکسان هستند

## 🚀 استفاده

### Laravel
```bash
cd /var/www/6ammart-laravel
php artisan serve
```

### React
```bash
cd /var/www/6ammart-react
npm install  # اگر نیاز باشد
npm run dev
```

## 🔄 همگام‌سازی مجدد

برای همگام‌سازی مجدد با سرور، می‌توانید از دستور زیر استفاده کنید:

```bash
# Laravel
sshpass -p 'H161t5dzCG' rsync -avz --delete \
  -e "ssh -o StrictHostKeyChecking=no" \
  root@193.162.129.214:/var/www/6ammart-laravel/ \
  /var/www/6ammart-laravel/

# React
sshpass -p 'H161t5dzCG' rsync -avz --delete \
  -e "ssh -o StrictHostKeyChecking=no" \
  root@193.162.129.214:/var/www/6ammart-react/ \
  /var/www/6ammart-react/
```

---
**وضعیت:** ✅ دانلود کامل از سرور  
**تطبیق:** ✅ ۱۰۰٪ مطابق با سرور  
**مسیرها:** ✅ دقیقاً یکسان  
**آماده برای استفاده:** ✅ بله

